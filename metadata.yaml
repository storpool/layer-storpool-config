name: layer-storpool-config
summary: Install the storpool-config package and generate storpool.conf
maintainer: StorPool <support@storpool.com>
description: |
  This layer installs a Debian package and writes out the main StorPool
  configuration file obtained from the Juju charm's configuration.
